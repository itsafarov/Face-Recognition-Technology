# Core dependencies
aiohttp>=3.8.0
aiofiles>=0.8.0
numpy>=1.21.0
Pillow>=8.3.0
opencv-python>=4.5.0
psutil>=5.8.0
pydantic>=1.8.0
pydantic-settings>=2.0.0

# Optional dependencies for reports
reportlab>=3.6.0  # For PDF reports
openpyxl>=3.0.0  # For Excel reports

# Optional performance enhancement
ujson>=4.0.0  # Faster JSON processing
uvloop>=0.17.0  # Faster asyncio on Unix systems (optional)

# Encoding detection
chardet>=4.0.0